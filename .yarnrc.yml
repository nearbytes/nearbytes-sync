nodeLinker: node-modules

# Yarn 4.14+ refuses to fetch any `git:` dependency whose URL doesn't
# match one of these patterns. Allow every Nearbytes repo on GitHub.
approvedGitRepositories:
  - "https://github.com/nearbytes/*"
  - "ssh://git@github.com/nearbytes/*"
